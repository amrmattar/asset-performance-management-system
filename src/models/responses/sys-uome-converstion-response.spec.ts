import { SysUomeConverstionResponse } from './sys-uome-converstion-response';

describe('SysUomeConverstionResponse', () => {
  it('should create an instance', () => {
    expect(new SysUomeConverstionResponse()).toBeTruthy();
  });
});
